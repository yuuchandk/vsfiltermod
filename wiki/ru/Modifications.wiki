#summary Список изменений в поведении относительно оригинального VSFilter.
#labels Featured

= Список изменений =

В поведении VSFilter выполнены следующие изменений:
 # Теперь доступно множественное использование тегов \fax, \fay в одном событии.
 # Задание отрицательного интервала между символами теперь работает из стиля.
 # VSFilterMod корректно загружает прикреплённые данные, если первый символ UUE кода "[".
 # Перемещаемый \org:
  * \org(x1,y1);
  * \org(x1,y1,x2,y2`[`,t1,t2`]`) —  перемещает базовую точку из (x1,y1) в (x2,y2) за интервал (t1,t2). Аналогично параметрам \move.